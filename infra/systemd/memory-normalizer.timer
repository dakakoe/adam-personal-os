[Unit]
Description=Run the raw → canonical normalizer every 5 minutes

[Timer]
# 2 min after boot to let docker + postgres settle, then every 5 min
# measured from when the previous run finished (Type=oneshot exits fast).
OnBootSec=2min
OnUnitActiveSec=5min
Persistent=true

[Install]
WantedBy=timers.target
