[Unit]
Description=Daily materialize of planned budget transactions

[Timer]
# 00:20 Asia/Bangkok (17:20 UTC) — just after the routines materializer (00:10),
# so a new day's recurring bills post early.
OnCalendar=*-*-* 17:20:00 UTC
Persistent=true

[Install]
WantedBy=timers.target
