[project]
name = "memory-digest"
version = "0.1.0"
description = "Daily contact-memory digest writer."
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.29",
    "httpx>=0.27",
]

[tool.uv]
package = false
