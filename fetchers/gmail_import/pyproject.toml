[project]
name = "memory-gmail-import"
version = "0.1.0"
description = "Streaming Gmail Takeout .mbox importer for the personal memory system."
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.29",
]

[tool.uv]
package = false
