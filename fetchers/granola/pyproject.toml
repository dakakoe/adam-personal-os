[project]
name = "memory-granola-fetcher"
version = "0.1.0"
description = "Droplet-native Granola poller: official API → merge ingest endpoint (replaces the Cowork relay)."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
]

[tool.uv]
package = false
