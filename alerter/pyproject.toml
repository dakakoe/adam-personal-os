[project]
name = "memory-alerter"
version = "0.1.0"
description = "Push-alert worker: notifies you on Telegram when the memory-os health degrades or Anthropic credits run dry."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "anthropic>=0.40",
]

[tool.uv]
package = false
