[project]
name = "memory-telegram-bot"
version = "0.1.0"
description = "Telegram capture bot: voice/text note → task / opportunity / calendar event (inline confirm)."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "faster-whisper>=1.0",
]

[tool.uv]
package = false
