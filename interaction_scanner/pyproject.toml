[project]
name = "memory-interaction-scanner"
version = "0.1.0"
description = "Scans recent 1:1 conversations for tasks/opportunities → suggestion inbox (Phase 3)."
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40",
    "asyncpg>=0.29",
    "httpx>=0.27",
]

[tool.uv]
package = false
