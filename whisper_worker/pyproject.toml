[project]
name = "memory-whisper"
version = "0.1.0"
description = "Local Whisper voice transcription worker (faster-whisper / CTranslate2)."
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.29",
    "faster-whisper>=1.0",
]

[tool.uv]
package = false
