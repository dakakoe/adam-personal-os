# Next.js build output + caches (regenerated by `next build` on droplet)
/.next/
/out/

# Dependencies (installed by npm/pnpm/yarn on the host that runs the app)
/node_modules/

# Misc
.DS_Store
.env.local
.env*.local
*.log
.vercel
*.tsbuildinfo
next-env.d.ts
