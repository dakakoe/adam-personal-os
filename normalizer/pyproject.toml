[project]
name = "memory-normalizer"
version = "0.1.0"
description = "Raw → canonical normalizer for the personal contact-memory system."
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.29",
]

[tool.uv]
package = false
