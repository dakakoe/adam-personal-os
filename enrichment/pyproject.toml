[project]
name = "memory-enrichment"
version = "0.1.0"
description = "Regex-driven identity/affiliation signal extractor for the personal memory system."
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.29",
]

[tool.uv]
package = false
