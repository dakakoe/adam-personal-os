# Point Rspamd's stateful modules (bayes stats, fuzzy, ratelimit, etc.) at the
# shared compose redis. Rspamd namespaces its own keys, so it coexists with the
# other redis users (Twenty, Authelia).
servers = "redis:6379";
